let selectedColors = ['red', 'blue']; //Square brackets [] Are called Array literals and they represent an empty array.
console.log(selectedColors);

console.log(selectedColors[0]);
console.log(selectedColors[1]);

selectedColors[2] = 'navy';
console.log(selectedColors);

/* Technically an Array is an object
An Array is a data structure that we use to represent a list of items.
*/